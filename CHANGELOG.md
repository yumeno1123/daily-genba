# 変更履歴 (CHANGELOG)

## [0.0.1] - 2026-06-08

### 追加
- 日付入力ボックスの右側に「-1日」「+1日」ボタンを追加し、1日ずつ簡単に日付を前後に切り替えられるようにしました。

### 変更
- CSV出力時に、入力されていない日付も含め、選択した月のすべての日のデータ（未入力日は空欄）が出力されるようにロジックを修正しました。

### 修正
- `src/services/db.ts` において、`IDBPDatabase` を値としてインポートしようとしてビルドエラーになっていた箇所を、型インポート (`import type`) に修正しました。これにより、`npm run build` が正常に通るようになりました。
